// minimum required temperature to bake a cookie
export const MIN_TEMPERATURE = 220;

// maximum temperature a cookie can be baked at
export const MAX_TEMPERATURE = 240;

// the amount with which the oven increases/decreases it's temperature 
export const TEMPERATURE_VARIATION = 10;
